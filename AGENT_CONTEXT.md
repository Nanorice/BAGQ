# Agent Context — For a New AI Session or a New Machine

> **The content of this file now lives in [`.claude/CLAUDE.md`](.claude/CLAUDE.md).**
> Read that. It is ~5 minutes and covers the user, the plan, the baseline, the design
> decisions not to re-open, current sprint status, working conventions, and how to help.

**Why the move:** Claude Code auto-loads `.claude/CLAUDE.md` into every session, so the context
arrives without anyone remembering to open a file. This pointer stays because the repo is
designed to be portable — clone it, and a human on a fresh machine or a non-Claude agent still
lands somewhere useful.

**If you are a human or a non-Claude agent:** open `.claude/CLAUDE.md` directly. It is plain
markdown with no tooling assumptions.

**Then read [`MANUAL.md`](MANUAL.md)** — the operating procedure: how a day, a stage and a sprint
actually run, and the rules that were paid for with real overruns. `CLAUDE.md` is *context*;
`MANUAL.md` is *procedure*.

---

*Moved 2026-07-30. Previously this file held the full context inline; keeping two copies in
sync was the failure mode being avoided.*
